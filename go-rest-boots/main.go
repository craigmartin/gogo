package main

import (
        "encoding/json"
        "flag"
        "fmt"
        "log"
        "net/http"

        "github.com/gorilla/mux"
)

// Error response contains everything we need to use http.Error


